# searchvisualizerdocker
Serving search algorithm visualizer using react and docker.

Build the Docker Image:
```
docker build . -t searchvisualizer
```

Run the Image:
```
docker run -p 3000:80 searchvisualizer
```
